package io.sarl.api.protocol.examples.cnp._generated.participant

capacity ParticipantInnerCapacity {

	def computeBid(task : String) : Integer
	
	def computeRefuseReason(task : String) : String
	
	def performTask() : boolean
	//TODO the boolean is not good to use a boolean
	// in this case it is ok, but what if we have more than two alternatives
	
	def computeFailureReason : String

}
